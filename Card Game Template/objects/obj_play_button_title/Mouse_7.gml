/// @description Do something when the button is pressed.
// Assuming the mouse is clicking on us,
if (clicked)
{
	if target_room!=noone
	{
		room_goto(target_room);
	}
	// Then the mouse button is released, go to the next screen.
}